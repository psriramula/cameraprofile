name: camera_profile
description: Quick App to see Camera Specs

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.7.0+2
  camera_features: ^0.0.2
  sensors: ^0.4.2+6
  path_provider:
  path: ^1.7.0

  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true